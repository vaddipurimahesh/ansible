- name: ansible conditions
  hosts: local
  connection: local
  vars:
    NAME: "nginx"
  tasks:
  - name: check if package is nginx"
    ansible.builtin.debug:
      msg: "Package is nginx"
    when: NAME == "nginx"